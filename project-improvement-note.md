## API calling improvement.
We currently developing a new API calling with server feature at `src\service` location.
Currently, the Buildable Land Space API calling is implemented at `src\service\boundary`
